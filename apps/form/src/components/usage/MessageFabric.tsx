import { UIMessage, UIDataTypes, UITools } from 'ai';
import {
  Message,
  MessageContent,
  MessageResponse,
} from '../ai-elements/message';

const messageSchema = {
  text: {
    type: 'text',
    ui: () => {},
  },
  'tool-input-available': {},
  'tool-output-available': {},
  'finish-step': {},
  finish: {},
};

export function MessageFabric({
  message,
}: {
  message: UIMessage<unknown, UIDataTypes, UITools>;
}) {
  return (
    <Message from={message.role} key={message.id}>
      <MessageContent>
        {message.parts.map((part, i) => {
          switch (part.type) {
            case 'text': // we don't use any reasoning or tool calls in this example
              return (
                <MessageResponse key={`${message.id}-${i}`}>
                  {part.text}
                </MessageResponse>
              );

            case 'tool-callSayHi':
              return <h1>{part.text}</h1>;
            default:
              return null;
          }
        })}
      </MessageContent>
    </Message>
  );
}

/* {
  "name": "callSayHi",
  "parameters": {
    "message": "{\"type\":\"string\",\"description\":\"приветственное сообщение\"} : \"Приветство!\""
  }
}

*/
/*


Привет! Я пишу задачу стажёру, чтобы понять, что он умеет. Помоги откорректировать и дополнить этот текст. Старайся держаться строго оригинала: нужно только сгладить углы, добавить пробелы, если они нужны, и выстроить логически понятный ход действий. Также добавь больше пунктов, которые ты считаешь важными в этой теме.

**Задача:**

Приведи пример кода всех стадий настройки tool calling для LLM чатов, основываясь на следующих библиотеках:
- `"ai": "^6.0.105"`
- `"@ai-sdk/react": "^3.0.107"`

### Важные аспекты, которые необходимо осветить:

1. **Общее описание подхода**
   - Что такое tool calling и зачем он нужен в чат-приложениях.
   - Как в библиотеке `ai` реализована поддержка инструментов (tools) и потоковой передачи ответов.

2. **Серверная часть (endpoint)**
   - Показать endpoint использовать hono ts, node `hono/api/chat/, который обрабатывает запрос от клиента использует как пример от которого
   нужно отталкиваться
   import {
  convertToModelMessages,
  UIMessage,
  streamText as streamTextAi,
} from 'ai';
   app.post('/chat', async ({ req }) => {
  const { messages, model }: { messages: UIMessage[]; model: string } =
    await req.json();

  const result = streamTextAi({
    model: ollama(model),
    messages: await convertToModelMessages(messages),
  });

  return result.toUIMessageStreamResponse();
});
.
   - Использование функции `streamText` для потоковой передачи ответа от LLM.
   - Настройка `streamText`: передача модели, сообщений, а главное – определение инструментов (tools), которые модель может вызывать.
   - Объяснить, как подключаются инструменты: описание схемы параметров (с помощью Zod), описание и название.
   - Пример простого инструмента (например, получение текущей погоды или поиск информации).
   - Как обрабатываются вызовы инструментов на сервере: через функцию `execute` (если выполнение происходит на сервере) и как результаты возвращаются в стрим.

3. **Детальное устройство инструмента (tool)**
   - Из чего состоит инструмент который передается?
   - Что происходит при вызове инструмента ?
   - Важно объяснить механизм связи между клиентом и LLM:
     - LLM может инициировать вызов инструмента, и этот вызов попадает на клиент через стрим.
     - Клиент (через `useChat`) получает уведомление о вызове и может выполнить инструмент на стороне клиента (например, показать UI, запросить подтверждение) и отправить результат обратно модели.
     - Этот цикл обеспечивает интерактивность и возможность выполнения действий как на сервере, так и на клиенте.

4. **Клиентская часть (React)**
   - Использование хука `useChat` из `@ai-sdk/react`.
   - Как настроить хук: указать API endpoint, обработчики событий.
   - Особенно важен обработчик `onToolCall`, который вызывается, когда модель запрашивает выполнение инструмента, не имеющего серверной `execute`.
   - Внутри `onToolCall` можно выполнить инструмент на клиенте (например, вызвать браузерное API, открыть модалку) и вернуть результат с помощью `toolCall` (или через `addToolResult`).
   - Пример отображения в чате:
     - Сообщения пользователя и ассистента.
     - Специальные компоненты для отображения вызовов инструментов (например, показать, что был вызван инструмент, и его результат).
     - Обработка состояния загрузки и ошибок.

5. **Дополнительные важные моменты**
   - Типизация всего процесса: использование TypeScript для инструментов и сообщений.
   - Обработка ошибок при выполнении инструментов (как на сервере, так и на клиенте).
   - Завершение стрима и корректная обработка финального ответа.
   - Возможность множественных вызовов инструментов в одном ответе.

Постарайся построить ответ в виде структурированного описания с примерами кода на каждый пункт. Код должен быть готов к использованию (с импортами, пояснениями). Если какие-то детали в оригинале были неочевидны – уточни их, но не отходи от основной идеи.

Спасибо!

*/
